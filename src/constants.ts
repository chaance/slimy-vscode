import * as path from "path";

export const CHARSET = "utf-8";
export const ROOT_DIR = path.resolve(__dirname, "..");
export const SRC_DIR = path.resolve(ROOT_DIR, "src");
export const THEMES_DIR = path.resolve(ROOT_DIR, "themes");
